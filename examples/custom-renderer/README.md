# Custom renderer

This example supplies a renderer for the index target in `vivliostyle.config.ts`.
It inserts a definition-list legend as the preamble and changes the default group structure from `div > section` to `ul > li`.
